version: '3'

services:
  go:
    build:
      context: .
      dockerfile: ./containers/go/Dockerfile
    tty: true
    volumes:
      - ./go:/go/src
  next:
    build:
      context: .
      dockerfile: ./containers/nextjs/Dockerfile
      target: dev
    tty: true
    volumes:
      - ./next:/nextjs
      - /nextjs/node_modules
      - /nextjs/.next
    ports:
      - 80:3000
    environment:
      - WATCHPACK_POLLING=true
